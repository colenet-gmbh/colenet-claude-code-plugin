# Claude Code local, per-project plugin settings (not for version control)
.claude/*.local.md
.claude/settings.local.json

# Vendored cape harness skills — this repo IS the source (skills_source/), so the
# /cape:setup snapshot in .claude/skills/ would only duplicate it. Ignored for now;
# whether to pin it is still open (see CONTEXT.md / team decision).
.claude/skills/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
